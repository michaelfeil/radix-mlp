# RadixMLP Train

Goal is to have a varlen python interface, using the flash-attn bindings and the roughtly same code as a mix of:
- varlen
- flash_attn_varlen
- radix-mlp enabled
- positional embeddings:

# Goal: Write modeling Qwen3ForCausaLLM with RadixMLP Varlen